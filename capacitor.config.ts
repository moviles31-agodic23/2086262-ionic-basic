import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'AppMov-2086262',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
